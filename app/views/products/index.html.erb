<p id="notice"><%= notice %></p>
<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>ID</th>
      <th>Item Name</th>
      <th>Item Type</th>
      <th>Brand</th>
      <th>Size</th>
      <th>Price</th>
      <th>Condition</th>
      <th colspan="1" id="actions">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <th scope="row"><%= product.id%></th>
        <td><%= product.name %></td>
        <td><%= product.item_type %></td>
        <td><%= product.brand %></td>
        <td><%= product.size %></td>
        <td><%= product.price %></td>
        <td><%= product.condition %></td>
        <td><%= link_to 'Show', product %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= paginate @products %>
